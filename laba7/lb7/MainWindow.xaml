<Window x:Class="lb7.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lb7"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="9*"/>

        </Grid.RowDefinitions>
        <Border  BorderBrush="Black" BorderThickness="1" Grid.Column="0" Grid.RowSpan="2" >
            <StackPanel>

                <TextBlock Text="Функціональні кнопки" FontWeight="Bold" FontSize="16" Margin="10"/>
                
                <Button Content="ДОДАТИ ЗАЯВКУ" Background="BlanchedAlmond" Command="{Binding AddButtonCommand}" />
                <Button Content="ЗМІНИТИ ІНФОРМАЦІЮ" Background="BlanchedAlmond" Command="{Binding ChangeButtonCommand}" CommandParameter="{Binding ElementName=StudentsList, Path=SelectedItems}" />
                <Button Content="ВИДАЛИТИ ЗАЯВКУ" ToolTip="ДЛЯ ВИДАЛЕННЯ ДЕКІЛЬКОХ АНКЕТ ЗАЖМІТЬ CTRL ТА ВИБЕРІТЬ ДЕКІЛЬКА АНКЕТ" Background="BlanchedAlmond" Command="{Binding DeleteButtonCommand}" CommandParameter="{Binding ElementName=StudentsList, Path=SelectedItems}" />
            </StackPanel>
        </Border>
        <Label  Grid.Column="1" Content="АНКЕТИ" FontSize="16" HorizontalContentAlignment="Center" HorizontalAlignment="Center" Grid.Row="0"/>
        <Label  Name="NOELEM" Grid.Column="1" Content="ЗАЯВКИ У ФАЙЛІ ВІДСУТНІ ДОДАЙТЕ НОВІ" FontSize="16" HorizontalContentAlignment="Center" HorizontalAlignment="Center" Grid.Row="1" Visibility="Hidden" />
        <ListBox x:Name="StudentsList" Grid.Column="1" Grid.Row="1" SelectedItem="{Binding currentStudent, Mode=TwoWay}" AlternationCount="2" SelectionMode="Extended" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto" ItemsSource="{Binding Students}" >
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="2"/>
                    <Setter Property="Padding" Value="0"/>

                    <Style.Triggers>
                        <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                            <Setter Property="Background" Value="PaleVioletRed"/>
                        </Trigger>

                        <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                            <Setter Property="Background" Value="AliceBlue"/>
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="LightGray"/>
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Orange"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel ItemWidth="125" ItemHeight="150" Margin="10,10,10,10" ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" Margin="5" Padding="10" Background="Transparent">

                        <Border.InputBindings>
                            <MouseBinding Gesture="LeftDoubleClick" 
                               Command="{Binding DataContext.CheckButtonCommand, 
                                         RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding ElementName=StudentsList, Path=SelectedItems}" />
                        </Border.InputBindings>

                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10">
                            <TextBlock Text="{Binding fullname}" FontWeight="Bold" TextAlignment="Center" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding AgeWithSuffix}" FontStyle="Italic" TextAlignment="Center" TextWrapping="Wrap" Foreground="Gray" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>

    </Grid>
</Window>
